import {createRequire} from 'node:module';import assert from 'node:assert/strict';const require=createRequire(import.meta.url);const {chromium}=require(process.env.PLAYWRIGHT_MODULE);const b=await chromium.launch();try{for(const theme of ['light','dark']){const p=await b.newPage({javaScriptEnabled:false,colorScheme:theme,viewport:{width:900,height:800}});await p.goto('http://127.0.0.1:53861/test/?firstpaint=59');assert.equal(await p.locator('body>.navigation').count(),1);const box=await p.locator('.navigation').boundingBox();assert.ok(box.y>650);assert.equal(await p.locator('.theme-toggle svg.jigokucho').count(),1);assert.equal(await p.locator('.avatar-slide').count(),2);await p.close()}console.log('PASS: initial HTML has bottom navbar, butterfly and portraits before JS');}finally{await b.close()}
