import {createRequire} from 'node:module';import assert from 'node:assert/strict';const require=createRequire(import.meta.url);const {chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const b=await chromium.launch({headless:true});try{const p=await b.newPage({viewport:{width:900,height:700},colorScheme:'dark'});await p.goto('http://127.0.0.1:53861/test/?nav=52');await p.waitForTimeout(1200);await p.mouse.move(200,200);await p.mouse.move(-1,-1);await p.locator('.soul-cursor[data-state=sit]').waitFor({state:'attached',timeout:20000});const z=await p.evaluate(()=>({sprite:+getComputedStyle(document.querySelector('.soul-cursor')).zIndex,nav:+getComputedStyle(document.querySelector('.navigation')).zIndex}));assert.ok(z.sprite>z.nav);await p.screenshot({path:'C:/Users/Vasudev Siddh/Documents/Codex/nav-rest.png'});console.log('PASS: resting character paints above bottom blur');}finally{await b.close();}
